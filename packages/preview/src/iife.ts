import { createGradient, mountWavrEditor, mountWavrPreview, presets, shouldShowEditor } from "./index";

const api = {
  createGradient,
  mountWavrEditor,
  mountWavrPreview,
  presets,
  shouldShowEditor,
};

export default api;
